# Feature-gated module trees the default feature set cannot compile.
# Every entry needs a justification; remove entries when the gate's
# cargo check learns to enable the feature.
#
# CUDA runtime is `#[cfg(feature = "cuda")] pub mod cuda;` and only builds
# where nvcc/CUDA 13.2 exist (aiwonder). On aiwonder the shared target dir
# usually contains cuda dep-info anyway; this entry keeps the gate exact on
# CPU-only machines.
crates/calyx-forge/src/cuda/

# cuVS CAGRA DiskANN backend is wired as
# `#[cfg(feature = "cuda")] mod cuvs_cagra;` under calyx-sextant. The default
# CPU workspace gate cannot compile optional cuVS deps, so the source is
# intentionally unreachable unless the cuda feature is enabled.
crates/calyx-sextant/src/index/diskann/cuvs_cagra.rs
